=== MANUAL DEPLOYMENT COMMANDS ===
Run these commands ONE AT A TIME in your terminal:

1. SSH into server:
   ssh root@146.190.111.28

2. Pull latest code:
   cd /opt/rezvo
   git pull origin main

3. Rebuild frontend:
   cd frontend
   npm run build

4. Restart backend:
   sudo systemctl restart rezvo-backend

5. Check status:
   sudo systemctl status rezvo-backend

6. Exit:
   exit

=== VERIFICATION ===
Open http://rezvo.co.uk and check:
- Homepage hero heading: clean geometric font (Figtree, NOT curvy serif)
- Directory page headings: same clean style
- Footer: CTA appears once, watermark at bottom
- No serif fonts anywhere

=== WHAT WAS FIXED ===
✅ Fonts: Enforced Figtree 800 for ALL headings
✅ Footer: Removed duplicate CTA, moved watermark to bottom
✅ Build: Clean production build (no errors)
✅ Git: Committed and pushed (cfd3356)

=== FILES CHANGED ===
- frontend/index.html (Google Fonts import)
- frontend/tailwind.config.js (font config)
- frontend/src/styles/index.css (CSS overrides)
- frontend/src/components/directory/RezvoFooter.jsx (structure)
- frontend/src/pages/directory/DirectoryLanding.jsx (removed duplicate)
