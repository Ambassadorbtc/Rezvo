╔═══════════════════════════════════════════════════════════════╗
║                  REZVO DEPLOYMENT — START HERE                 ║
╚═══════════════════════════════════════════════════════════════╝

YOUR LOCAL SSH IS HANGING. Run commands DIRECTLY on the server.

═══════════════════════════════════════════════════════════════

STEP 1: SSH into server
────────────────────────────────────────────────────────────────
ssh root@146.190.111.28


STEP 2: Run this ONE command to diagnose everything
────────────────────────────────────────────────────────────────
sudo certbot certificates && echo "---" && sudo cat /etc/nginx/sites-enabled/rezvo 2>/dev/null || sudo cat /etc/nginx/sites-enabled/default && echo "---" && sudo systemctl status rezvo-backend --no-pager && echo "---" && ss -tlnp | grep 8000


STEP 3: Based on output, pick ONE fix
────────────────────────────────────────────────────────────────

IF you see "No certificates found":
→ Run: sudo certbot --nginx -d rezvo.co.uk -d www.rezvo.co.uk

IF you see certificate but nginx has no "listen 443 ssl":
→ Run: sudo nano /etc/nginx/sites-enabled/rezvo
→ Add the HTTPS server block (see rezvo-nginx-https.conf)
→ Save, then: sudo nginx -t && sudo systemctl reload nginx

IF backend shows "inactive" or "failed":
→ Run: sudo systemctl restart rezvo-backend


STEP 4: Add Anthropic API key
────────────────────────────────────────────────────────────────
sudo nano /opt/rezvo/backend/.env

Add this line at the bottom:
ANTHROPIC_API_KEY=sk-ant-YOUR-ACTUAL-KEY-HERE

Save: Ctrl+O, Enter, Ctrl+X

Restart: sudo systemctl restart rezvo-backend


STEP 5: Pull latest code (fonts + footer fixes)
────────────────────────────────────────────────────────────────
cd /opt/rezvo
git pull origin main
cd frontend
npm run build


STEP 6: Test everything
────────────────────────────────────────────────────────────────
curl -I https://rezvo.co.uk
# Should return: HTTP/2 200

Open in browser: https://rezvo.co.uk
# Should show SSL padlock


═══════════════════════════════════════════════════════════════

DETAILED GUIDES AVAILABLE:
- BLOCKER-FIXES-COMPLETE-GUIDE.md (full walkthrough)
- COPY-PASTE-FIXES.txt (quick commands)
- DEPLOYMENT-STATUS-SUMMARY.md (current state)

═══════════════════════════════════════════════════════════════

REPORT BACK:
1. Did HTTPS start working? (yes/no)
2. Did API key get added? (yes/no)
3. Any errors? (paste them)

Then we'll move to Prompt 2.

═══════════════════════════════════════════════════════════════
